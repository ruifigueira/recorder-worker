{
	"name": "recorder-worker",
	"version": "0.0.1",
	"private": true,
	"scripts": {
		"build:injected": "vite build --config vite.injected.config.ts",
		"deploy": "npm run build:injected && wrangler deploy",
		"dev": "npm run build:injected && wrangler dev",
		"start": "npm run build:injected && wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/playwright": "^0.0.11",
		"@cloudflare/puppeteer": "^1.0.4",
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"rrweb": "^2.0.0-alpha.4",
		"typescript": "^5.5.2",
		"vite": "^7.1.3",
		"vitest": "~3.2.0",
		"wrangler": "^4.31.0"
	},
	"dependencies": {
		"@cloudflare/flame-common": "^4.1.1",
		"@cloudflare/stagehand": "file:../stagehand/cloudflare",
		"hono": "^4.9.2",
		"workers-ai-provider": "^0.7.5"
	}
}
